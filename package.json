{
  "name": "inline-markdown",
  "version": "0.1.1",
  "license": "MIT",
  "bin": {
    "inline-markdown": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "tslint src/**/*.ts && ava-ts src/**/*.test.ts",
    "build": "tsc",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^10.12.9",
    "@types/recursive-readdir": "^2.2.0",
    "ava": "^0.25.0",
    "ava-ts": "^0.25.2",
    "tslint": "^5.11.0",
    "typescript": "^3.1.6"
  },
  "dependencies": {
    "@types/mkdirp": "^0.5.2",
    "@types/yargs": "^12.0.1",
    "mkdirp": "^0.5.1",
    "recursive-readdir": "^2.2.2",
    "yargs": "^12.0.5"
  }
}
